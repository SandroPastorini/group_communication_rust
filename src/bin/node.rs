fn main() {
    /**
     * 1. anunciarse en el grupo por la dirección multicast
     * 2. escuchar en la dirección multicast
     * 3. leer de la consola input del usuario
     * 4. mandar mensaje a dirección multicast -- ver que no haya "acoplamiento", que no reciba su propio mensaje
     * 5. esperar acuse de recibo de todos los nodos? >> a resolver.
     * 6. imprimir en pantalla confirmación
     */
}